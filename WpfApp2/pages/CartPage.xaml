<Page x:Class="WpfApp2.pages.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CartPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Кнопка "Назад" -->
        <Button Grid.Row="0"
                Content="← Назад к товарам"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="0,0,0,10"
                Padding="12,6"
                Click="BackToProducts_Click"/>

        <!-- Список корзины -->
        <ListView Grid.Row="1" x:Name="CartList">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Товар" DisplayMemberBinding="{Binding Product.Product_Name}" Width="200"/>
                    <GridViewColumn Header="Цена" DisplayMemberBinding="{Binding Product.Price, StringFormat={}{0:C}}" Width="100"/>
                    <GridViewColumn Header="Количество" DisplayMemberBinding="{Binding Count}" Width="80"/>
                    <GridViewColumn Header="Действие" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Удалить" 
                                        Click="RemoveItem_Click" 
                                        Tag="{Binding Product.ID}" 
                                        Padding="10,2"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Кнопка оформления -->
        <Button Grid.Row="2"
                Content="Перейти к оформлению"
                HorizontalAlignment="Right"
                Margin="0,10,0,0"
                Padding="20,8"
                Click="GoToCheckout_Click"/>
    </Grid>
</Page>
