<Page x:Class="WpfApp1.pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Background="#FFFFFF" Margin="0,0,0,10">
            <TextBox x:Name="SearchTextBox" Width="200" Margin="10" TextChanged="SearchTextBox_TextChanged"/>
            <ComboBox x:Name="SortComboBox" Width="150" Margin="10" SelectionChanged="SortComboBox_SelectionChanged">
                <ComboBoxItem Content="Название"/>
                <ComboBoxItem Content="Рейтинг"/>
            </ComboBox>
            <Button Content="Личная страница" Click="PersonalPageButton_Click" Margin="10"/>
            <Button Content="Вход" Click="LoginButton_Click" Margin="10"/>
            <Button Content="Регистрация" Click="RegisterButton_Click" Margin="10"/>
        </StackPanel>

        <ListView x:Name="MoviesListView" Grid.Row="1" ItemsSource="{Binding Movies}" MouseDoubleClick="MoviesListView_MouseDoubleClick" Background="Transparent">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" Margin="10" Padding="10" CornerRadius="5">
                        <StackPanel>
                            <Image Source="{Binding CoverImage}" Width="100" Height="150"/>
                            <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Rating}"/>
                            <TextBlock Text="{Binding ReleaseDate}"/>
                            <TextBlock Text="{Binding AgeRating.Age}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
