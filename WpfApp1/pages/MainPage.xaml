<Page x:Class="WpfApp1.pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Background="#FFFFFF" Margin="0,0,0,10">
            <TextBox x:Name="SearchTextBox" Width="200" Margin="10" TextChanged="SearchTextBox_TextChanged"/>
            <ComboBox x:Name="SortComboBox" Width="150" Margin="10" SelectionChanged="SortComboBox_SelectionChanged">
                <ComboBoxItem Content="Название"/>
                <ComboBoxItem Content="Рейтинг"/>
            </ComboBox>
            <Button Content="Личная страница" Click="PersonalPageButton_Click" Margin="10"/>
            <Button Content="Вход" Click="LoginButton_Click" Margin="10"/>
            <Button Content="Регистрация" Click="RegisterButton_Click" Margin="10"/>
        </StackPanel>

        <ListView x:Name="MoviesListView" Grid.Row="1" ItemsSource="{Binding Movies}" MouseDoubleClick="MoviesListView_MouseDoubleClick" Background="Transparent">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Width" Value="200"/>
                    <Setter Property="Height" Value="300"/>
                    <Setter Property="Margin" Value="10"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="5">
                        <Grid>
                            <Image Source="{Binding CoverImage}" Stretch="Fill"/>
                            <Border Background="#80000000" VerticalAlignment="Bottom" Padding="10">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="Bold" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Rating}" Foreground="White"/>
                                    <TextBlock Text="{Binding ReleaseDate}" Foreground="White"/>
                                    <TextBlock Text="{Binding AgeRating.Age}" Foreground="White"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
