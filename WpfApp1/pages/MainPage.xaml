<Page x:Class="WpfApp1.pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBox x:Name="SearchTextBox" Width="200" Margin="10"/>
            <Button Content="Поиск" Click="SearchButton_Click" Margin="10"/>
            <ComboBox x:Name="SortComboBox" Width="150" Margin="10">
                <ComboBoxItem Content="Название"/>
                <ComboBoxItem Content="Рейтинг"/>
            </ComboBox>
            <Button Content="Сортировать" Click="SortButton_Click" Margin="10"/>
            <Button Content="Личная страница" Click="PersonalPageButton_Click" Margin="10"/>
            <Button Content="Вход" Click="LoginButton_Click" Margin="10"/>
            <Button Content="Регистрация" Click="RegisterButton_Click" Margin="10"/>
        </StackPanel>

        <ListView x:Name="MoviesListView" Grid.Row="2" ItemsSource="{Binding Movies}" MouseDoubleClick="MoviesListView_MouseDoubleClick">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="10">
                        <Image Source="{Binding CoverImage}" Width="100" Height="150"/>
                        <TextBlock Text="{Binding Title}"/>
                        <TextBlock Text="{Binding Rating}"/>
                        <TextBlock Text="{Binding ReleaseDate}"/>
                        <TextBlock Text="{Binding AgeRating.Age}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
