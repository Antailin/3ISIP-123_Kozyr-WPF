<Page x:Class="WpfApp1.pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage">

    <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" Margin="100" Padding="20" CornerRadius="10" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel>
            <TextBlock Text="Имя пользователя" FontWeight="Bold"/>
            <TextBox x:Name="UsernameTextBox" Margin="0,5,0,10"/>
            <TextBlock Text="Пароль" FontWeight="Bold"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,5,0,10"/>
            <Button Content="Вход" Click="LoginButton_Click" Margin="0,10,0,5"/>
            <Button Content="Назад" Click="BackButton_Click" Margin="0,5,0,0"/>
        </StackPanel>
    </Border>
</Page>
