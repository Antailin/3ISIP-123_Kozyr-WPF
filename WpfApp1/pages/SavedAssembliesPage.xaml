<Page x:Class="WpfApp1.pages.SavedAssembliesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SavedAssembliesPage">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Margin="0,0,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Список сборок"
                       FontSize="15" FontWeight="Bold" Margin="0,0,0,6"/>

            <ListBox Grid.Row="1" x:Name="LbAssemblies"
                     SelectionChanged="LbAssemblies_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,4">
                            <TextBlock Text="{Binding name}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding author}" Foreground="Gray" FontSize="11"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Button Grid.Row="2" Content="🗑 Удалить сборку"
                    Click="DeleteAssembly_Click"
                    Margin="0,6,0,0" Background="#F8D7DA"/>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" x:Name="TxtAssemblyTitle"
                       Text="Выберите сборку слева"
                       FontSize="15" FontWeight="Bold" Margin="0,0,0,2"/>

            <TextBlock Grid.Row="1" x:Name="TxtAssemblyAuthor"
                       FontSize="12" Foreground="Gray" Margin="0,0,0,8"/>

            <ListView Grid.Row="2" x:Name="LvAssemblyParts">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="" Width="54">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Source="{Binding ImageUrl}"
                                           Width="44" Height="44"
                                           Stretch="Uniform"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Тип" Width="120"
                                        DisplayMemberBinding="{Binding TypeName}"/>
                        <GridViewColumn Header="Название" Width="190"
                                        DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Производитель" Width="120"
                                        DisplayMemberBinding="{Binding Manufacturer}"/>
                        <GridViewColumn Header="Цена" Width="90"
                                        DisplayMemberBinding="{Binding PriceFormatted}"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
                <TextBlock Text="Итого: " FontSize="14" FontWeight="Bold"/>
                <TextBlock x:Name="TxtSavedTotal" FontSize="14"
                           FontWeight="Bold" Foreground="DarkGreen"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
