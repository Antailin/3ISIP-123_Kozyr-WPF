<Page x:Class="WpfApp1.pages.SelectPizzaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SelectPizzaPage">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Меню от шефа Гордова" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>

            <ListBox x:Name="PizzaList" Height="300" SelectionChanged="PizzaList_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{Binding Description}" FontStyle="Italic" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding BasePrice, StringFormat=Цена: {0}}" Foreground="Green"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Button x:Name="BtnNext" Content="Далее" Margin="0,20,0,0" Height="40" 
                    Click="BtnNext_Click" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Page>
